{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/tansey-lab/cellconsensus/main/nextflow/assets/schema_input.json",
    "title": "nf-core/cellconsensus pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample_id": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample ID must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "adata_path": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.h5ad$",
                "errorMessage": "AnnData file path must be provided, cannot contain spaces and must have extension '.h5ad'"
            }
        },
        "required": ["sample_id", "adata_path"]
    }
}
